// ============================================================================
//  Portal.h - 网页配置门户（softAP + WebServer）
//  开机时若未配置，或按住 BOOT 按钮上电，进入配置门户。
//  通过浏览器访问 192.168.4.1 填写 WiFi、和风 API Key、城市后保存重启。
// ============================================================================
#pragma once
#include <Arduino.h>
#include "AppConfig.h"

// 交互式配置门户。阻塞运行，直到用户保存配置（随后自动重启）。
// cur: 当前已保存的配置（用于页面预填）；preserve: 是否保留已有 key/城市。
void portalEnter(const AppConfig& current);